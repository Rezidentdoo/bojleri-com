# Self-hosted test mod — BEZ Vercel Blob-a
# Za test: cp .env.selfhosted.example .env.selfhosted && cp .env.selfhosted .env.local && npm run dev
# Za povratak na Vercel mod: cp backups/env/.env.local.backup .env.local

CMS_PASSWORD=bojleri2026
CMS_SECRET=your-random-secret-key-min-32-chars
CRON_SECRET=your-cron-secret

SMTP_HOST=mail.bojleri.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=prodaja@bojleri.com
SMTP_PASS=
SMTP_TLS_SERVERNAME=mail.bojleri.com
ORDER_FROM_EMAIL="bojleri.com <prodaja@bojleri.com>"
ORDER_NOTIFY_EMAIL=prodaja@bojleri.com

# Namerno BEZ BLOB_READ_WRITE_TOKEN